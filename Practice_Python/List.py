# 1. Create a list of 5 integers.
# 2. Create a list of 3 strings.
# 3. Access the first element of a list.
# 4. Access the last element of a list.
# 5. Change the second element of a list to 10.
# 6. Print the third element of ['a','b','c','d'].
# 7. Create a list of 4 elements and print the length.
# 8. Print the type of a list.
# 9. Access the middle element of a 5-element list.
# 10. Swap the first and last element of a list.
# 11. Slice the first 3 elements from a list.
# 12. Slice the last 2 elements from a list.
# 13. Slice elements from index 2 to 4.
# 14. Slice every second element of a list.
# 15. Reverse a list using slicing.
# 16. Slice a list from index 1 to end.
# 17. Slice a list from start to index 3.
# 18. Slice only odd-indexed elements.
# 19. Slice only even-indexed elements.
# 20. Slice the last 3 elements using negative indices.
# 21. Add an element to the end of a list using append().
# 22. Add multiple elements to a list using extend().
# 23. Insert an element at index 2 using insert().
# 24. Create a list and add [10,20,30] to it.
# 25. Add a string to a list using append().
# 26. Insert a number at the beginning of a list.
# 27. Append a list as an element to another list.
# 28. Extend a list with another list of strings.
# 29. Use + operator to join two lists.
# 30. Add an empty list to a list.
# 31. Remove an element using remove().
# 32. Remove the last element using pop().
# 33. Remove an element at index 1 using pop().
# 34. Delete a list element using del.
# 35. Delete the entire list using del.
# 36. Remove all elements using clear().
# 37. Remove an element only if it exists using remove().
# 38. Pop the first element of a list.
# 39. Remove duplicates manually using slicing (no loops).
# 40. Remove an element using negative index and pop().
# 41. Find the index of an element using index().
# 42. Count the number of times an element appears using count().
# 43. Check if an element exists using in.
# 44. Check if an element does not exist using not in.
# 45. Count the number of times 5 appears in [1,5,5,2,5].
# 46. Find the index of last element (manually using length).
# 47. Find index of first occurrence of 'a' in ['a','b','a'].
# 48. Check if 'Python' is in a list of strings.
# 49. Count the number of strings with length 3 in ['cat','dog','bat'].
# 50. Find index of maximum element using index(max(list)).
# 51. Sort a list in ascending order using sort().
# 52. Sort a list in descending order using sort(reverse=True).
# 53. Create a sorted copy of a list using sorted().
# 54. Reverse a list using reverse().
# 55. Reverse a list using slicing.
# 56. Sort a list of strings alphabetically.
# 57. Sort a list of strings in reverse alphabetical order.
# 58. Find the max element of a list.
# 59. Find the min element of a list.
# 60. Swap first and last element and then sort the list.
# 61. Copy a list using copy().
# 62. Copy a list using slicing.
# 63. Join two lists using +.
# 64. Join two lists using extend().
# 65. Create a list and make an independent copy.
# 66. Join three lists into one list.
# 67. Flatten a list of lists using slicing (without loops).
# 68. Concatenate [1,2] and [3,4].
# 69. Copy first 3 elements into a new list.
# 70. Create a new list from last 2 elements of a list.
# 71. Multiply a list by 2.
# 72. Check if 10 is in [5,10,15].
# 73. Add two lists element-wise (manually without loops).
# 74. Find sum of all elements using sum().
# 75. Find the length of [1,2,3,4].
# 76. Find max value of [2,7,3].
# 77. Find min value of [2,7,3].
# 78. Find average of [2,4,6,8].
# 79. Reverse [1,2,3] using slicing.
# 80. Swap first and second elements without loop.
# 81. Access first element of nested list [[1,2],[3,4]].
# 82. Access 4 from [[1,2],[3,4]].
# 83. Change 2 to 5 in [[1,2],[3,4]].
# 84. Flatten a 2-element nested list manually.
# 85. Add a new list as element in a nested list.
# 86. Remove a sublist using del.
# 87. Copy a nested list shallowly using copy().
# 88. Access last element of last sublist.
# 89. Count number of sublists in a nested list using len().
# 90. Concatenate two nested lists.
# 91. Replace last element with 100.
# 92. Access elements using negative indexing.
# 93. Slice last 3 elements of a list.
# 94. Slice first 2 elements of a list.
# 95. Check if list is empty using len().
# 96. Clear all elements using clear().
# 97. Count length of string inside list.
# 98. Create a list from tuple using list().
# 99. Convert list of numbers to list of strings using map(str, ...).
# 100. Access middle element of an odd-length list.